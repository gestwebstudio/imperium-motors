# Imperium Motors — страница каталога: план и сводка для продолжения

Точка входа для нового чата: **«Прочитай docs/catalog-progress.md и продолжай оттуда».**

- **Репозиторий:** `gestwebstudio/imperium-motors`, ветка `claude/homepage-final`.
- **Figma каталога:** node `578-1927`
  (https://www.figma.com/design/bERRa0eUHmPImiBdiP62UR/Imperium-Motors?node-id=578-1927)
- **git-идентичность:** `gest / gest.webstudio@gmail.com`;
  коммиты заканчивать `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`.

## ⚠️ Условия работы
1. **UI-тесты не гонять** — проверять быстрыми замерами (Playwright: getBoundingClientRect / getComputedStyle), одноразовый скрипт `_m.mjs` в корне (после прогона удалять).
2. **Скриншоты** — можно снимать через Playwright (`locator.screenshot`) и смотреть; браузерная панель localhost блокирует.
3. **Общаться по-русски.**
4. **Коммитить И пушить после каждой задачи** самому: `git add` → commit → `git fetch` → `git rebase origin/claude/homepage-final` → `git push`. Только локальный коммит не считается (заказчик смотрит ветку на GitHub).
5. Сообщение коммита — через файл `-F` (в Bash-туле здесь PowerShell here-string `@'…'@` НЕ работает).
6. **Параллельно коллега** работает в `src/app/**` (HeroUI). Каталог тоже в `src/app` — согласовать/ребейзить перед пушем.

## Стек / решения (подтверждено заказчиком)
- Страница — **React в `src/app`** (маршрут `src/app/(site)/catalog/page.tsx`), на **HeroUI** (`@heroui/react`, уже установлен). shadcn НЕ ставим.
- «Подключённая библиотека» = **HeroUI**. Компоненты: `Breadcrumbs`, `Dropdown`/`Select`, `Accordion`, `Checkbox`, `Slider`.
- `car-card` (сейчас статический HTML/CSS в `public/ui-kit`) **портировать в React-компонент** на наших токенах.
- Данные карточек — **мок-массив**, догрузка — **имитация**.
- Диапазоны: цена **4 500 000 … 50 000 000 ₽**, мощность **100 … 900 л.с.**
- Стек проекта: Next 16 / React 19 / Tailwind v4 / HeroUI / Prisma.

## Scope — только 5 блоков (не весь макет)
Крошки → заголовок → сортировка → фильтры → карточки с догрузкой.

### 1. Хлебные крошки
`Breadcrumbs` (HeroUI). Путь ≈ `Главная / Каталог` (метки уточнить по Figma).

### 2. Заголовок
Заголовок страницы каталога (текст/размер — из Figma).

### 3. Сортировка
Дропдаун HeroUI. Пункты ровно три:
- по возрастанию цены
- по убыванию цены
- по популярности

### 4. Фильтры
**Блок выбранных фильтров (сверху):**
- показывается **только когда выбран хотя бы один фильтр** ниже (иначе скрыт);
- каждый выбранный фильтр — чип **тега типа Filter** (`2026 ×`, уже есть в ките); по **×** удаляется один;
- кнопка **«Сброс»** — удаляет все фильтры.

**Сами фильтры (HeroUI):**
- раскрываются **аккордеоном** (`Accordion`), внутри — выбор **чекбоксами (галочками)**;
- наборы характеристик в секциях — **рандомные, придумать самому** (марка, кузов, топливо, привод, КПП, год, цвет и т.п.);
- секции **«Цена»** и **«Мощность»** — кастомный компонент:
  - **ползунок из HeroUI** (`Slider`, двусторонний), **поля ввода над ним — кастомные**;
  - левое поле — маска начала диапазона `от 4 500 0..`, правое — `до 50 000 0..` (число видимых цифр подобрать под ширину поля);
  - **двусторонняя синхронизация:** ввод числа в поле двигает соответствующий бегунок; перетаскивание бегунка меняет число в поле; левое ↔ левый, правое ↔ правый;
  - «Мощность» — та же механика, но в **л.с.**

**Прилипание и внутренний скролл блока фильтров:**
- блок **`position: sticky`**, при скролле страницы вниз прикрепляется к верху окна;
- **`top` при прилипании = вертикальному зазору между верхом блока фильтров и заголовком над ним** (значение снять из Figma);
- если блок выше вьюпорта — **внутренний скролл**: `max-height: calc(100vh − top − нижний_зазор)` + `overflow-y: auto`.

### 5. Карточки
Карточки авто (React-порт). **Infinite scroll** — автодогрузка при скролле вниз; несколько рядов, чтобы догрузка срабатывала несколько раз.

## Порядок шагов
1. Снять точные спеки блоков из Figma `578-1927` (лейаут, отступы, цвета, размеры, зазор заголовок↔фильтры).
2. Маршрут `src/app/(site)/catalog/page.tsx` + HeroUI-провайдер (если ещё не подключён).
3. Портировать `car-card` в React-компонент на токенах.
4. Крошки + заголовок.
5. Сортировка (дропдаун, 3 пункта, стейт).
6. Фильтры: аккордеон + чекбоксы → стейт выбранных; sticky + внутренний скролл.
7. Кастомные «Цена»/«Мощность» (Slider + поля-маски, двусторонняя синхронизация).
8. Блок выбранных фильтров (условный рендер, чипы-Filter с ×, «Сброс»).
9. Сетка карточек + infinite scroll.
10. Замеры/проверка, коммит+пуш по этапам.

## Прогресс
- [ ] 1. Спеки из Figma
- [ ] 2. Маршрут + провайдер
- [ ] 3. car-card → React
- [ ] 4. Крошки + заголовок
- [ ] 5. Сортировка
- [ ] 6. Фильтры (аккордеон, sticky)
- [ ] 7. Слайдеры цена/мощность
- [ ] 8. Блок выбранных фильтров
- [ ] 9. Карточки + infinite scroll
